# models/sources.yml
version: 2

sources:
  - name: raw
    database: ZOMATO_DB
    schema: RAW
    description: "Raw CSV data loaded from files"
    tables:
      - name: raw_countries
      - name: raw_foods
      - name: raw_users
      - name: raw_restaurant
      - name: raw_orders
      - name: raw_menu

